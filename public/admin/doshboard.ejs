<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dika Knit — Админ</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* ======================================== */
        /*  СТИЛИ ДЛЯ АДМИНА                       */
        /* ======================================== */
        
        .admin-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Верхняя панель */
        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 20px;
            background: rgba(255,255,255,0.03);
            border-bottom: 1px solid rgba(201,169,89,0.08);
            margin-bottom: 24px;
            flex-wrap: wrap;
            gap: 10px;
        }
        .top-bar .logo {
            font-size: 22px;
            font-weight: 800;
            color: #c9a959;
        }
        .top-bar .logo span {
            color: #fff;
            font-weight: 300;
        }
        .top-bar .nav-links {
            display: flex;
            align-items: center;
            gap: 16px;
            flex-wrap: wrap;
        }
        .top-bar .nav-links a {
            color: #8892b0;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            transition: 0.2s;
        }
        .top-bar .nav-links a:hover {
            color: #c9a959;
        }
        .top-bar .nav-links a.active {
            color: #c9a959;
            font-weight: 600;
        }
        .top-bar .user-info {
            display: flex;
            align-items: center;
            gap: 12px;
            color: #8892b0;
            font-size: 14px;
        }
        .top-bar .user-info .avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: #c9a959;
            color: #0a0e1a;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 14px;
        }
        .top-bar .user-info .logout {
            color: #f87171;
            text-decoration: none;
            font-weight: 600;
        }
        .top-bar .user-info .logout:hover {
            text-decoration: underline;
        }

        /* Статистика */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 16px;
            margin-bottom: 24px;
        }
        .stat-card {
            background: rgba(255,255,255,0.04);
            border-radius: 16px;
            padding: 20px;
            text-align: center;
            border: 1px solid rgba(201,169,89,0.08);
        }
        .stat-card .icon {
            font-size: 28px;
            display: block;
            margin-bottom: 4px;
        }
        .stat-card .value {
            font-size: 32px;
            font-weight: 800;
            color: #c9a959;
        }
        .stat-card .label {
            color: #8892b0;
            font-size: 13px;
            margin-top: 4px;
        }

        /* Создание задания */
        .create-task {
            background: rgba(255,255,255,0.04);
            padding: 24px;
            border-radius: 16px;
            border: 1px solid rgba(201,169,89,0.08);
            margin-bottom: 24px;
        }
        .create-task h2 {
            color: #c9a959;
            margin-bottom: 16px;
            font-size: 20px;
            font-weight: 700;
        }
        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
            align-items: end;
        }
        .form-group {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }
        .form-group label {
            font-size: 12px;
            color: #8892b0;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .form-group input,
        .form-group select {
            padding: 12px 16px;
            background: rgba(255,255,255,0.07);
            border: 1px solid rgba(201,169,89,0.1);
            border-radius: 10px;
            color: #fff;
            font-size: 14px;
            transition: 0.3s;
            font-family: inherit;
        }
        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: #c9a959;
            background: rgba(255,255,255,0.1);
        }
        .form-group input::placeholder {
            color: #475569;
        }
        .form-group select option {
            background: #1a1f36;
            color: #fff;
        }
        .btn-submit {
            padding: 12px 24px;
            background: linear-gradient(135deg, #c9a959, #b89840);
            border: none;
            border-radius: 10px;
            font-weight: 700;
            font-size: 15px;
            color: #0a0e1a;
            cursor: pointer;
            transition: 0.3s;
            font-family: inherit;
        }
        .btn-submit:hover {
            transform: scale(1.02);
            box-shadow: 0 8px 30px rgba(201,169,89,0.35);
        }

        /* Таблица */
        .tasks-table {
            background: rgba(255,255,255,0.03);
            border-radius: 16px;
            overflow: hidden;
            border: 1px solid rgba(201,169,89,0.05);
        }
        .tasks-table .table-toolbar {
            display: flex;
            gap: 12px;
            padding: 12px 16px;
            flex-wrap: wrap;
            align-items: center;
            background: rgba(255,255,255,0.03);
            border-bottom: 1px solid rgba(201,169,89,0.05);
        }
        .tasks-table .table-toolbar .search-input {
            flex: 1;
            min-width: 200px;
            padding: 8px 14px;
            background: rgba(255,255,255,0.06);
            border: 1px solid rgba(201,169,89,0.1);
            border-radius: 8px;
            color: #fff;
            font-size: 14px;
            font-family: inherit;
        }
        .tasks-table .table-toolbar .search-input:focus {
            outline: none;
            border-color: #c9a959;
        }
        .tasks-table .table-toolbar .search-input::placeholder {
            color: #475569;
        }
        .tasks-table .table-toolbar .badge {
            color: #8892b0;
            font-size: 13px;
        }
        .tasks-table .table-toolbar .badge strong {
            color: #4ade80;
        }
        .tasks-table .table-toolbar .btn-export {
            padding: 6px 16px;
            background: linear-gradient(135deg, #4ade80, #22c55e);
            border: none;
            border-radius: 6px;
            color: #0a0e1a;
            font-weight: 600;
            cursor: pointer;
            font-size: 13px;
            font-family: inherit;
        }
        .tasks-table .table-toolbar .btn-export:hover {
            transform: scale(1.02);
        }
        .tasks-table .table-toolbar .btn-delete-completed {
            padding: 6px 16px;
            background: #f87171;
            border: none;
            border-radius: 6px;
            color: #fff;
            font-weight: 600;
            cursor: pointer;
            font-size: 13px;
            font-family: inherit;
        }
        .tasks-table .table-toolbar .btn-delete-completed:hover {
            background: #ef4444;
        }
        .tasks-table table {
            width: 100%;
            border-collapse: collapse;
        }
        .tasks-table th {
            text-align: left;
            padding: 12px 16px;
            background: rgba(201,169,89,0.05);
            color: #c9a959;
            font-weight: 600;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .tasks-table td {
            padding: 10px 16px;
            border-bottom: 1px solid rgba(255,255,255,0.04);
            color: #ddd;
            font-size: 13px;
        }
        .tasks-table tr:hover td {
            background: rgba(255,255,255,0.03);
        }
        .tasks-table .status-badge {
            padding: 2px 12px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 600;
            display: inline-block;
        }
        .tasks-table .status-pending {
            background: rgba(255,193,7,0.15);
            color: #ffc107;
        }
        .tasks-table .status-in_progress {
            background: rgba(96,165,250,0.15);
            color: #60a5fa;
        }
        .tasks-table .status-completed {
            background: rgba(74,222,128,0.15);
            color: #4ade80;
        }
        .tasks-table .status-archived {
            background: rgba(255,255,255,0.05);
            color: #475569;
        }
        .tasks-table .coat-parts-info .part {
            display: inline-block;
            background: rgba(201,169,89,0.1);
            padding: 0 6px;
            border-radius: 4px;
            margin: 0 2px;
            font-size: 10px;
            color: #c9a959;
        }
        .tasks-table .progress-bar-small {
            display: inline-block;
            width: 60px;
            height: 4px;
            background: rgba(255,255,255,0.08);
            border-radius: 2px;
            overflow: hidden;
            vertical-align: middle;
        }
        .tasks-table .progress-bar-small .fill {
            height: 100%;
            background: linear-gradient(90deg, #c9a959, #e8c97a);
            border-radius: 2px;
            transition: width 0.3s;
        }
        .tasks-table .action-btns {
            display: flex;
            gap: 4px;
            flex-wrap: wrap;
        }
        .tasks-table .action-btns .btn-action {
            padding: 3px 10px;
            border: none;
            border-radius: 4px;
            font-size: 12px;
            cursor: pointer;
            font-family: inherit;
            transition: 0.2s;
        }
        .tasks-table .action-btns .btn-action:hover {
            transform: scale(1.05);
        }
        .tasks-table .action-btns .btn-edit {
            background: #c9a959;
            color: #0a0e1a;
        }
        .tasks-table .action-btns .btn-duplicate {
            background: #60a5fa;
            color: #fff;
        }
        .tasks-table .action-btns .btn-delete {
            background: #f87171;
            color: #fff;
        }

        /* Адаптив */
        @media (max-width: 768px) {
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .form-grid {
                grid-template-columns: 1fr;
            }
            .tasks-table {
                overflow-x: auto;
            }
            .tasks-table table {
                min-width: 700px;
                font-size: 12px;
            }
            .tasks-table th,
            .tasks-table td {
                padding: 8px 10px;
            }
            .top-bar {
                flex-direction: column;
                align-items: stretch;
                text-align: center;
            }
            .top-bar .nav-links {
                justify-content: center;
            }
            .top-bar .user-info {
                justify-content: center;
            }
            .tasks-table .table-toolbar {
                flex-direction: column;
                align-items: stretch;
            }
        }
        @media (max-width: 480px) {
            .stats-grid {
                grid-template-columns: 1fr 1fr;
                gap: 8px;
            }
            .stat-card .value {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>

    <!-- ======================================== -->
    <!--  ВЕРХНЯЯ ПАНЕЛЬ                         -->
    <!-- ======================================== -->
    <div class="top-bar">
        <div class="logo">Dika <span>Knit</span></div>
        <div class="nav-links">
            <a href="/admin" class="active">📋 Задания</a>
            <a href="/admin/models">🧵 Модели</a>
            <a href="/admin/colors">🎨 Цветы</a>
            <a href="/admin/workers">👥 Сотрудники</a>
            <a href="/admin/shifts">📊 Смены</a>
            <a href="/admin/efficiency">⏱️ Эффективность</a>
        </div>
        <div class="user-info">
            <span class="avatar"><%= user.fullName ? user.fullName.charAt(0).toUpperCase() : 'A' %></span>
            <span><%= user.fullName || 'Админ' %></span>
            <a href="/profile" style="color:#4ade80; text-decoration:none;">🔑</a>
            <a href="/logout" class="logout">🚪 Выйти</a>
        </div>
    </div>

    <!-- ======================================== -->
    <!--  ОСНОВНОЙ КОНТЕНТ                       -->
    <!-- ======================================== -->
    <div class="admin-container">

        <!-- ======================================== -->
        <!--  📊 СТАТИСТИКА                          -->
        <!-- ======================================== -->
        <div class="stats-grid">
            <div class="stat-card">
                <span class="icon">📋</span>
                <div class="value"><%= tasks ? tasks.length : 0 %></div>
                <div class="label">Всего заданий</div>
            </div>
            <div class="stat-card">
                <span class="icon">✅</span>
                <div class="value"><%= tasks ? tasks.filter(t => t.status === 'completed').length : 0 %></div>
                <div class="label">Выполнено</div>
            </div>
            <div class="stat-card">
                <span class="icon">🔄</span>
                <div class="value"><%= tasks ? tasks.filter(t => t.status === 'in_progress').length : 0 %></div>
                <div class="label">В работе</div>
            </div>
            <div class="stat-card">
                <span class="icon">⏳</span>
                <div class="value"><%= tasks ? tasks.filter(t => t.status === 'pending').length : 0 %></div>
                <div class="label">В очереди</div>
            </div>
        </div>

        <!-- ======================================== -->
        <!--  ➕ СОЗДАНИЕ ЗАДАНИЯ                   -->
        <!-- ======================================== -->
        <div class="create-task">
            <h2>➕ Создать задание</h2>
            <form action="/api/tasks" method="post" class="form-grid" id="taskForm">
                <div class="form-group">
                    <label>📋 Модель</label>
                    <select name="modelId" required>
                        <option value="">Выберите модель...</option>
                        <% if (models && models.length > 0) { %>
                            <% models.forEach(m => { %>
                                <option value="<%= m.id %>"><%= m.name %> (<%= m.program %>) <%= m.isCoat ? '🧥' : '' %></option>
                            <% }); %>
                        <% } %>
                    </select>
                </div>
                <div class="form-group">
                    <label>🎨 Цвет</label>
                    <select name="colorId" required>
                        <option value="">Выберите цвет...</option>
                        <% if (colors && colors.length > 0) { %>
                            <% colors.forEach(c => { %>
                                <option value="<%= c.id %>"><%= c.name %></option>
                            <% }); %>
                        <% } %>
                    </select>
                </div>
                <div class="form-group">
                    <label>➕ Доп. цвет</label>
                    <select name="colorId2">
                        <option value="">Без доп. цвета</option>
                        <% if (colors && colors.length > 0) { %>
                            <% colors.forEach(c => { %>
                                <option value="<%= c.id %>"><%= c.name %></option>
                            <% }); %>
                        <% } %>
                    </select>
                </div>
                <div class="form-group">
                    <label>🏢 ИП</label>
                    <select name="ip" required>
                        <option value="">Выберите ИП...</option>
                        <option value="ИП Алла">ИП Алла</option>
                        <option value="ИП Заур">ИП Заур</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>📦 Количество</label>
                    <input type="number" name="planQuantity" placeholder="шт." min="1" required>
                </div>
                <div class="form-group" style="flex-direction:row; align-items:center; gap:8px;">
                    <input type="checkbox" name="isUrgent" id="urgent" style="width:18px; height:18px;">
                    <label for="urgent" style="color:#f87171; font-weight:600; text-transform:none;">🔥 Срочно</label>
                </div>
                <button type="submit" class="btn-submit">💾 Создать</button>
            </form>
        </div>

        <!-- ======================================== -->
        <!--  📋 ТАБЛИЦА ЗАДАНИЙ                    -->
        <!-- ======================================== -->
        <div class="tasks-table">
            <div class="table-toolbar">
                <input type="text" class="search-input" id="taskSearch" placeholder="🔍 Поиск по заданиям...">
                <span class="badge">Найдено: <strong id="searchCount">0</strong></span>
                <span style="flex:1;"></span>
                <span class="badge">✅ Готовых: <strong id="completedCount" style="color:#4ade80;">0</strong></span>
                <button class="btn-export" onclick="exportCompleted()">📊 Excel</button>
                <button class="btn-delete-completed" onclick="deleteCompleted()">🗑️ Удалить готовые</button>
            </div>
            <table>
                <thead>
                    <tr>
                        <th>Модель</th>
                        <th>Программа</th>
                        <th>Размер</th>
                        <th>Цвет</th>
                        <th>ИП</th>
                        <th>Кол-во</th>
                        <th>Связано</th>
                        <th>Статус</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody id="tasksBody">
                    <% if (tasks && tasks.length > 0) { %>
                        <% tasks.forEach(task => { 
                            const isCoat = task.isCoat || false;
                            let done = task.doneQuantity || 0;
                            let plan = task.planQuantity || 0;
                            let partsHtml = '';
                            if (isCoat && task.parts) {
                                plan = 0;
                                done = 0;
                                task.parts.forEach(p => {
                                    plan += p.planQuantity || 0;
                                    done += Math.min(p.doneQuantity || 0, p.planQuantity || 0);
                                });
                                partsHtml = task.parts.map(p => `<span class="part">${p.partName || 'Деталь'}</span>`).join(' ');
                            }
                            const percent = plan > 0 ? Math.min((done / plan) * 100, 100) : 0;
                            const statusClass = task.status === 'completed' ? 'status-completed' : 
                                                task.status === 'in_progress' ? 'status-in_progress' : 
                                                task.status === 'archived' ? 'status-archived' : 'status-pending';
                            const statusText = task.status === 'completed' ? '✅ Готов' :
                                               task.status === 'in_progress' ? '🔄 В работе' :
                                               task.status === 'archived' ? '📦 Архив' : '⏳ Ожидает';
                            const searchData = [
                                task.Model ? task.Model.name : '',
                                task.Model ? task.Model.program : '',
                                task.Color ? task.Color.name : '',
                                task.ip || '',
                                task.planQuantity || 0,
                                done,
                                isCoat ? 'кофта' : 'шапка'
                            ].join(' ').toLowerCase();
                        %>
                        <tr class="task-row" data-search="<%= searchData %>" data-task-id="<%= task.id %>">
                            <td>
                                <%= task.Model ? task.Model.name : '—' %>
                                <% if (isCoat) { %>
                                    <span style="font-size:10px; color:#c9a959; display:block;">
                                        🧥 Кофта
                                        <span class="coat-parts-info"><%= partsHtml %></span>
                                    </span>
                                <% } %>
                                <% if (task.isUrgent) { %>
                                    <span style="color:#f87171; font-size:11px; font-weight:700;">🔥</span>
                                <% } %>
                            </td>
                            <td><%= task.Model ? task.Model.program : '—' %></td>
                            <td><%= task.Model ? task.Model.size : '—' %></td>
                            <td><%= task.Color ? task.Color.name : '—' %></td>
                            <td><%= task.ip || '—' %></td>
                            <td><%= plan %></td>
                            <td>
                                <%= done %> / <%= plan %>
                                <div class="progress-bar-small">
                                    <div class="fill" style="width: <%= percent %>%;"></div>
                                </div>
                                <span style="font-size:10px; color:#475569; margin-left:4px;"><%= Math.round(percent) %>%</span>
                            </td>
                            <td><span class="status-badge <%= statusClass %>"><%= statusText %></span></td>
                            <td>
                                <div class="action-btns">
                                    <button class="btn-action btn-edit" onclick="editTask(<%= task.id %>)">✏️</button>
                                    <form action="/api/tasks/duplicate/<%= task.id %>" method="post" style="display:inline;">
                                        <button type="submit" class="btn-action btn-duplicate">🔄</button>
                                    </form>
                                    <form action="/api/tasks/delete/<%= task.id %>" method="post" style="display:inline;" onsubmit="return confirm('Удалить задание #<%= task.id %>?')">
                                        <button type="submit" class="btn-action btn-delete">🗑️</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        <% }); %>
                    <% } else { %>
                        <tr><td colspan="9" style="text-align:center; color:#475569; padding:40px;">📭 Нет заданий</td></tr>
                    <% } %>
                </tbody>
            </table>
        </div>

    </div>

    <!-- ======================================== -->
    <!--  МОДАЛКА РЕДАКТИРОВАНИЯ                 -->
    <!-- ======================================== -->
    <div id="editModal" class="modal">
        <div class="modal-content" style="max-width:600px; max-height:90vh; overflow-y:auto;">
            <h2>✏️ Редактирование заказа #<span id="editTaskIdDisplay"></span></h2>
            <form id="editForm">
                <input type="hidden" id="editTaskId">
                
                <div style="background:rgba(255,255,255,0.03); padding:12px 16px; border-radius:10px; margin-bottom:15px;">
                    <div style="display:flex; flex-wrap:wrap; gap:8px 16px; font-size:13px; color:#8892b0;">
                        <span>📄 Модель: <strong style="color:#fff;" id="editModelName">—</strong></span>
                        <span>🎨 Цвет: <strong style="color:#fff;" id="editColorName">—</strong></span>
                        <span>🏢 ИП: <strong style="color:#fff;" id="editIp">—</strong></span>
                    </div>
                    <div style="margin-top:8px;">
                        <div style="display:flex; justify-content:space-between; font-size:13px; color:#8892b0;">
                            <span>Прогресс</span>
                            <span><strong style="color:#fff;" id="editDone">0</strong> из <strong style="color:#c9a959;" id="editPlan">0</strong> шт. (<span id="editPercent">0</span>%)</span>
                        </div>
                        <div class="progress-bar" style="height:6px; background:rgba(255,255,255,0.08); border-radius:3px; overflow:hidden; margin-top:4px;">
                            <div id="editProgressFill" style="height:100%; background:linear-gradient(90deg, #c9a959, #e8c97a); border-radius:3px; width:0%; transition:width 0.3s;"></div>
                        </div>
                    </div>
                </div>

                <div id="editShapkiFields">
                    <div class="form-group" style="margin-bottom:10px;">
                        <label>📦 Количество (план)</label>
                        <input type="number" id="editQuantity" name="quantity" min="0" required>
                    </div>
                    <div class="form-group">
                        <label>🔧 Связано (факт)</label>
                        <input type="number" id="editDoneQuantity" name="doneQuantity" min="0">
                        <div style="font-size:11px; color:#8892b0; margin-top:4px;">
                            Операций в системе: <strong id="editOpsCount">0</strong> шт.
                        </div>
                    </div>
                </div>

                <div id="editCoatFields" style="display:none;">
                    <div style="color:#c9a959; font-weight:600; margin-bottom:8px; font-size:13px;">🧥 Детали кофты</div>
                    <div id="editCoatPartsFields" style="display:grid; grid-template-columns:1fr 1fr; gap:8px;"></div>
                </div>

                <div class="form-group" style="margin-top:15px;">
                    <label>📌 Статус</label>
                    <select id="editStatus" name="status">
                        <option value="pending">⏳ Ожидает</option>
                        <option value="in_progress">🔄 В работе</option>
                        <option value="completed">✅ Готово</option>
                    </select>
                </div>

                <div style="display:flex; gap:10px; margin-top:20px;">
                    <button type="submit" class="btn-submit" style="flex:1;">💾 Сохранить</button>
                    <button type="button" onclick="closeEditModal()" style="flex:1; padding:12px; background:rgba(255,255,255,0.08); border:none; border-radius:10px; color:#fff; cursor:pointer;">❌ Отмена</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        // ========================================
        //  ПОИСК ПО ТАБЛИЦЕ
        // ========================================
        document.addEventListener('DOMContentLoaded', function() {
            const searchInput = document.getElementById('taskSearch');
            const rows = document.querySelectorAll('.task-row');
            const searchCount = document.getElementById('searchCount');
            const completedCount = document.getElementById('completedCount');

            function updateCounts() {
                let visible = 0;
                let completed = 0;
                rows.forEach(row => {
                    const search = row.getAttribute('data-search') || '';
                    const filter = searchInput.value.toLowerCase().trim();
                    const isVisible = search.includes(filter);
                    row.style.display = isVisible ? '' : 'none';
                    if (isVisible) visible++;
                    
                    const statusEl = row.querySelector('.status-completed');
                    if (statusEl) completed++;
                });
                if (searchCount) searchCount.textContent = visible;
                if (completedCount) completedCount.textContent = completed;
            }

            if (searchInput) {
                searchInput.addEventListener('input', updateCounts);
            }
            updateCounts();

            // Обновляем счётчик готовых каждые 3 секунды
            setInterval(updateCounts, 3000);
        });

        // ========================================
        //  РЕДАКТИРОВАНИЕ
        // ========================================
        async function editTask(taskId) {
            const modal = document.getElementById('editModal');
            document.getElementById('editTaskId').value = taskId;
            document.getElementById('editTaskIdDisplay').textContent = taskId;
            modal.classList.add('active');

            try {
                const res = await fetch('/api/tasks/' + taskId + '/edit-data');
                const data = await res.json();
                if (!data.success) { alert('❌ ' + (data.error || 'Ошибка')); return; }

                const task = data.task;
                const isCoat = task.isCoat || false;
                const parts = task.parts || [];
                const totalDone = data.totalDone || 0;
                const totalPlan = data.totalPlan || task.planQuantity || 0;
                const percent = totalPlan > 0 ? Math.min((totalDone / totalPlan) * 100, 100) : 0;

                document.getElementById('editModelName').textContent = task.Model?.name || '—';
                document.getElementById('editColorName').textContent = task.Color?.name || '—';
                document.getElementById('editIp').textContent = task.ip || '—';
                document.getElementById('editDone').textContent = totalDone;
                document.getElementById('editPlan').textContent = totalPlan;
                document.getElementById('editPercent').textContent = Math.round(percent);
                document.getElementById('editProgressFill').style.width = percent + '%';
                document.getElementById('editStatus').value = task.status || 'pending';

                if (isCoat && parts.length > 0) {
                    document.getElementById('editShapkiFields').style.display = 'none';
                    document.getElementById('editCoatFields').style.display = 'block';

                    const container = document.getElementById('editCoatPartsFields');
                    container.innerHTML = '';
                    parts.forEach(p => {
                        const div = document.createElement('div');
                        div.className = 'form-group';
                        div.innerHTML = `
                            <label style="font-size:11px; color:#8892b0;">
                                ${p.partName || 'Деталь'} (связано: ${p.doneQuantity || 0})
                            </label>
                            <input type="number" name="part_${p.id}_plan" value="${p.planQuantity || 0}" min="0" style="width:100%;">
                            <input type="number" name="part_${p.id}_done" value="${p.doneQuantity || 0}" min="0" style="width:100%; margin-top:4px;">
                        `;
                        container.appendChild(div);
                    });
                } else {
                    document.getElementById('editShapkiFields').style.display = 'block';
                    document.getElementById('editCoatFields').style.display = 'none';
                    document.getElementById('editQuantity').value = task.planQuantity || 0;
                    document.getElementById('editDoneQuantity').value = totalDone || 0;
                    document.getElementById('editOpsCount').textContent = (task.operations || []).length || 0;
                }
            } catch (e) { alert('❌ ' + e.message); }
        }

        function closeEditModal() {
            document.getElementById('editModal').classList.remove('active');
        }

        document.getElementById('editForm')?.addEventListener('submit', async function(e) {
            e.preventDefault();
            const taskId = document.getElementById('editTaskId').value;
            const status = document.getElementById('editStatus').value;
            const isCoat = document.getElementById('editCoatFields').style.display !== 'none';
            const data = { status };

            if (isCoat) {
                const inputs = document.querySelectorAll('#editCoatPartsFields input');
                const parts = {};
                inputs.forEach(input => {
                    const name = input.getAttribute('name');
                    if (name) parts[name] = parseInt(input.value) || 0;
                });
                data.parts = parts;
            } else {
                data.quantity = parseInt(document.getElementById('editQuantity').value) || 0;
                data.doneQuantity = parseInt(document.getElementById('editDoneQuantity').value) || 0;
            }

            try {
                const res = await fetch('/api/tasks/edit/' + taskId, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });
                const result = await res.json();
                if (result.success) {
                    alert('✅ Изменения сохранены!');
                    closeEditModal();
                    location.reload();
                } else {
                    alert('❌ ' + (result.error || 'Ошибка'));
                }
            } catch (e) { alert('❌ ' + e.message); }
        });

        // ========================================
        //  ЭКСПОРТ И УДАЛЕНИЕ ГОТОВЫХ
        // ========================================
        function getCompletedOrders() {
            const rows = document.querySelectorAll('.task-row');
            const result = { data: [], ids: [] };
            rows.forEach(row => {
                const statusEl = row.querySelector('.status-completed');
                if (statusEl) {
                    const cells = row.querySelectorAll('td');
                    if (cells.length >= 8) {
                        result.data.push({
                            'Модель': cells[0]?.textContent?.trim() || '—',
                            'Программа': cells[1]?.textContent?.trim() || '—',
                            'Размер': cells[2]?.textContent?.trim() || '—',
                            'Цвет': cells[3]?.textContent?.trim() || '—',
                            'ИП': cells[4]?.textContent?.trim() || '—',
                            'План': cells[5]?.textContent?.trim() || '0',
                            'Связано': cells[6]?.textContent?.trim() || '0',
                            'Статус': 'Готово'
                        });
                        const id = row.dataset.taskId;
                        if (id) result.ids.push(id);
                    }
                }
            });
            return result;
        }

        function exportCompleted() {
            const result = getCompletedOrders();
            if (result.data.length === 0) { alert('Нет готовых заказов'); return; }
            if (typeof XLSX === 'undefined') { alert('Библиотека XLSX не загружена'); return; }
            const wb = XLSX.utils.book_new();
            const ws = XLSX.utils.json_to_sheet(result.data);
            XLSX.utils.book_append_sheet(wb, ws, 'Готовые заказы');
            const date = new Date().toISOString().split('T')[0];
            XLSX.writeFile(wb, 'gotovye-zakazy-' + date + '.xlsx');
        }

        function deleteCompleted() {
            const result = getCompletedOrders();
            if (result.ids.length === 0) { alert('Нет готовых заказов'); return; }
            if (!confirm('Удалить ' + result.ids.length + ' готовых заказов?')) return;
            
            let deleted = 0, errors = 0;
            let i = 0;
            function deleteNext() {
                if (i >= result.ids.length) {
                    alert('✅ Удалено ' + deleted + ' заказов' + (errors ? ', ошибок: ' + errors : ''));
                    location.reload();
                    return;
                }
                fetch('/api/tasks/delete/' + result.ids[i], { method: 'POST' })
                    .then(r => { if (r.ok) deleted++; else errors++; })
                    .catch(() => errors++)
                    .finally(() => { i++; setTimeout(deleteNext, 100); });
            }
            deleteNext();
        }

        // Закрытие модалки по клику вне
        window.onclick = function(e) {
            if (e.target.classList.contains('modal')) {
                e.target.classList.remove('active');
            }
        };
    </script>

</body>
</html>
